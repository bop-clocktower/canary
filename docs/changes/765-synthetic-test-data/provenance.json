{
  "issues": [765],
  "route": "spec-ready",
  "stages": [
    "autopilot",
    "harness-planner",
    "harness-task-executor",
    "harness-code-reviewer"
  ],
  "specArtifact": "docs/changes/765-synthetic-test-data/proposal.md",
  "plan": "docs/changes/765-synthetic-test-data/plans/2026-09-16-gen-data-v1-slice-plan.md",
  "assumptions": [
    "Slice = spec implementation steps 2+3, schema-first per 'D2 revisited': JSON Schema source, ShapeNode, seeded generator, vitest emitter, canary gen-data CLI",
    "Only a JSON Schema subset resolves; $ref, type arrays, allOf, conditionals, tuple items and null become unresolved with a reason",
    "Fields counted at leaves; array items once (paths like lines[].sku)",
    "Fixture name from schema title, else file name",
    "Only top-level unresolved keys get the overrides-required throw; nested ones are listed in the file header",
    "C1: blackhawk/savant scanners unloadable -> exit 3, nothing written",
    "C2: resolved non-object root -> exit 2; unresolved or union root ($ref, allOf, anyOf) -> exit 3",
    "pytest framework -> exit 2 'not yet supported in this slice'",
    "Default seed 765, mulberry32 PRNG, 50-case cap",
    "Arch module-size floor moved to measured 41825 with allowance file; no ceiling or maxFindings raised",
    "Planner skeleton sign-off taken autonomously by the orchestrator (fleet lane, recommended defaults)"
  ],
  "closingKeyword": "Refs",
  "reason": "Partial delivery: Python extractor + pytest emitter, edge-case --format json + case planner, compiler-API and zod sources, ADRs for D2/D7, and test-author guidance remain (spec steps 4-6)."
}
