{
  "issues": [965],
  "route": "feature",
  "stages": ["harness:brainstorming"],
  "planArtifact": "docs/changes/remove-oracle-comments/plans/2026-09-15-remove-oracle-comments-plan.md",
  "closingKeyword": "Closes",
  "countBefore": 27,
  "countAfter": 2,
  "testsOracleCount": 29,
  "assumptions": [
    "harness:brainstorming was invoked and its EXPLORE phase was run. Its own When-to-Use excludes simple refactors that need no design decisions, so the EVALUATE/PRIORITIZE/sign-off gates were settled by the fleet's recommended defaults and the fixed scope instead of a live human interview. No proposal.md was written.",
    "harness:autopilot was NOT invoked: this is single-phase, comment-only work with no plan phases to chain. It is therefore not listed in stages.",
    "core/company-knowledge.ts:872 is a runtime string literal, the voice flavor line (#340), shown unless CANARY_NO_FLAVOR/NO_FLAVOR is set. The lane parked on it because changing it changes user-visible output. The human then decided to replace 'Oracle: eyes on every test.' with 'Canary: eyes on every test.'. The orchestrator made that change and its comment at :871, and pinned the exact text in ts/test/company-knowledge.test.ts (previously only toBeTruthy). With it, #965 is fully resolved, so the closing keyword is Closes. This is the one intentional user-visible change in the PR.",
    "core/permission-matrix.ts:21,79 say 'existence-oracle', a security term for a probe that reveals whether a resource exists. That is not the retired brand, so both were kept.",
    "Every rewrite is same-line, so the net line delta is 0. The diff touches only comment lines, checked by a grep of changed lines against comment prefixes.",
    "ts/test has 29 'oracle' lines. They were not edited (out of scope) and are left as a follow-up."
  ]
}
