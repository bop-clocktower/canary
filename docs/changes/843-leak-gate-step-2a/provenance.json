{
  "issues": [843],
  "route": "spec-ready",
  "stages": ["autopilot", "security-review"],
  "adr": "docs/knowledge/decisions/0023-leak-gate-pull-request-target.md",
  "spec": "docs/changes/843-leak-gate-pull-request-target/proposal.md",
  "plan": "docs/changes/843-leak-gate-step-2a/plans/2026-09-25-leak-gate-step-2a-plan.md",
  "closingKeyword": "Refs",
  "assumptions": [
    "Spec-ready lane: the Amendment in the #843 plan defines step 2a, so brainstorming was skipped and autopilot ran against the existing spec and ADR 0023",
    "Autopilot ran at fast rigor (one low-complexity phase, 5 tasks, plan auto-approved); VERIFY was harness validate plus the four ts/ gates, REVIEW dispatched harness-code-reviewer and harness-security-reviewer",
    "The manifest's required entry keeps workflow docs-lint.yml in 2a, because that is the producer that reports the context on the 2a PR itself; 2b moves it",
    "Two check runs with one context name from the same app (github-actions) are acceptable for the 2a to 2b window: the ruleset entry has no integration_id and on PR #1112 the pull_request_target run attached to the PR head SHA. How GitHub resolves two same-named runs (latest-wins is the usual report) is UNVERIFIED; the known cost is order-dependent results for fork PRs and scanner-editing PRs until 2b, with no protection weaker than main today",
    "The transitional advisory entry is removed in 2a, since no workflow produces that name after the rename; the name was never in the ruleset, and pull_request_target always runs current main's copy, so once 2a merges no PR reports it any more",
    "AGENTS.md does not name the leak-gate job, so it needs no change in 2a",
    "The job body from runs-on onward is byte-identical to main; only the name and its comments change"
  ]
}
