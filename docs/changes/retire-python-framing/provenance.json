{
  "issues": [926],
  "route": "feature",
  "stages": ["brainstorming"],
  "planArtifact": "docs/changes/retire-python-framing/plans/2026-09-15-retire-python-framing-narrow-plan.md",
  "closingKeyword": "Closes",
  "scope": {
    "inScopeFiles": [
      "ts/src/analysis/cli.ts",
      "ts/src/core/skill-registry.ts",
      "ts/src/core/test-files.ts",
      "ts/src/core/workflow-discovery.ts",
      "ts/src/guardian/diff-coverage/formats/cobertura.ts",
      "ts/src/guardian/diff-coverage/formats/coverage-json-lint.ts",
      "ts/src/guardian/diff-extractor.ts",
      "ts/src/guardian/pr-comment.ts",
      "ts/src/mcp-server.ts",
      "ts/src/ui/banner.ts",
      "ts/src/util/ensure-ascii.ts",
      ".github/workflows/harness-quality.yml"
    ],
    "outOfScopeSitesFound": 38
  },
  "assumptions": [
    "Scope narrowed by the human to the sites Issue #926 enumerates (groups 1-3). Group 4 (spike/schemathesis keep-or-delete) is a decision, not a rename, and is left open for a follow-up.",
    "harness:brainstorming was invoked, but its interactive gates (clarifying questions, 2-3 approaches, section-by-section sign-off, proposal.md) were not run: the lane is autonomous and the scope was already settled by the human. Recommended defaults were taken.",
    "harness:autopilot was loaded but NOT run: its own When-to-Use excludes single-phase work, and its PLAN/EXECUTE/VERIFY/REVIEW subagent dispatches and OUTCOME_EVAL did not happen. It is therefore not listed in stages. The plan was written directly and executed inline.",
    "harness:refactoring was not invoked as a skill; its discipline (gates green before and after, no behavior change) was followed by hand.",
    "Inside the 11 files only the port framing was removed. Private py* helper copies in those files other than diff-extractor.ts (pyTruthy, pyGet, pyRepr, pySplitlines, pySuffix, pyBracketsBalanced) keep their names: renaming them is the 'py* copies in other modules' follow-up.",
    "Comments containing 'oracle' were left untouched (#965). In diff-extractor.ts, isNone was renamed to isAbsent along with the 6 named helpers because it is the same helper family.",
    "The MCP tool description string in mcp-server.ts that names agent/frameworks/registry.json was left: changing it changes MCP output, so it is not zero-behavior-change."
  ]
}
