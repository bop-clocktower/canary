{
  "reason": "Personas as a first-class engine concept (#462) plus the consumer wiring #341 never had: module-size 29130->29693 (+563). Attributed empirically rather than arithmetically, the way #686's allowance was: moving ts/src/core/persona.ts aside and re-running `harness check-arch --json` drops currentValue to 29172, so that one new file is 521 of the 563 and the ts/src/mcp-server.ts resolution call site is the remaining 42 (a +47/-3 diff; module-size and `git diff` disagree by a line or two on blank-line handling, which is why the number recorded here is the measured one). Nothing else in the diff contributes -- ts/test/persona.test.ts and the ts/test/mcp-server.test.ts additions are test code and module-size does not count them, and the rest is CHANGELOG.md, AGENTS.md, and two skill markdown files. REBASED twice: first recorded as 28634->29128 off 45d16b4, then re-measured off 3717f07 after #699 and #702 merged (#702's own allowance moved the allowed base to 29130). Only the absolute base moved; this change's own contribution grew from 494 to 563 because the two-independent-signal floor landed in between. Per #689, the arrow in a check-arch verdict is never a delta -- the left operand is baselines.json, and every allowance merged since it was refreshed sits inside that gap. The growth buys the removal of a duplicated-and-already-drifted abstraction rather than adding one: canary-edge-case-discovery documented `--level sdet|junior|manual` with its own inference rules in prose, ts/src/core/environment-detect.ts returned `sdet|manual|unknown` from a different heuristic, and no code read either -- the detected level was computed on every analyze_file call and dropped. Of the 521 lines roughly 230 are comment and doc block, deliberately: five of this module's design choices are decisions a later reader would otherwise reverse (voice is a separate axis owned by voice/discovery.md; the fallback is explanatory rather than terse or maximal; the resolver reads no environment variable so its caller owns that I/O; the confidence floor cannot work alone because confidence is a margin and one unopposed signal scores 1.0; and independence is counted by signal KIND so ten open TypeScript files are one observation restated). Each is pinned by a test as well as explained next to the code it constrains. The remainder is a validating loader, a lookup pair, the three-step resolution cascade with its provenance strings, the JSON view, and the overlay merge that rides the existing #333 precedence contract. Splitting would not help -- module-size is a repo-wide aggregate, so N files summing to 563 lines score identically to one, and the file is well under the per-file thresholds. Nothing was absorbed here to avoid a fix: newViolations is 0, the pre-existing violations stay at 21, and the entropy count is 295 against main's 296 -- one BELOW main, because wiring detectUserLevel into the analyze_file persona path gave a previously unreferenced export a consumer. No entropy entry point was declared and maxFindings was not touched.",
  "categories": {
    "module-size": 29693
  },
  "violationIds": [],
  "createdFrom": "3717f07"
}
