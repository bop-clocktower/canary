{
  "reason": "Personas as a first-class engine concept (#462) plus the consumer wiring #341 never had: module-size 28634->29128 (+494). Attributed empirically rather than arithmetically, the way #686's allowance was: moving ts/src/core/persona.ts aside and re-running `harness check-arch --json` drops currentValue to 28662, so that one new file is 466 of the 494 and the ts/src/mcp-server.ts resolution call site is the remaining 28 (+31/-2 lines). Nothing else in the diff contributes -- ts/test/persona.test.ts (576 lines) and the ts/test/mcp-server.test.ts additions are test code and module-size does not count them, and the rest is CHANGELOG.md, AGENTS.md, and two skill markdown files. The growth buys the removal of a duplicated-and-already-drifted abstraction rather than adding one: canary-edge-case-discovery documented `--level sdet|junior|manual` with its own inference rules in prose, ts/src/core/environment-detect.ts returned `sdet|manual|unknown` from a different heuristic, and no code read either -- the detected level was computed on every analyze_file call and dropped. Of the 466 lines roughly 200 are comment and doc block, deliberately: three of this module's design choices are decisions a later reader would otherwise reverse (voice is a separate axis owned by voice/discovery.md, the fallback is explanatory rather than terse or maximal, and the resolver reads no environment variable so its caller owns that I/O), and each is recorded next to the code it constrains as well as pinned by a test. The remainder is a validating loader, a lookup pair, the three-step resolution cascade with its provenance strings, the JSON view, and the overlay merge that rides the existing #333 precedence contract. Splitting would not help -- module-size is a repo-wide aggregate, so N files summing to 466 lines score identically to one, and the file is well under the per-file thresholds. Nothing was absorbed here to avoid a fix: newViolations is 0, the pre-existing violations stay at 21, and the entropy count is 294 -- identical to main -- after narrowing the module's exported surface from eleven values to three rather than declaring a new entropy entry point or touching maxFindings.",
  "categories": {
    "module-size": 29128
  },
  "violationIds": [],
  "createdFrom": "45d16b4"
}
