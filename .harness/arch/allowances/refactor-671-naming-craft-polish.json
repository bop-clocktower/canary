{
  "reason": "Naming-craft polish findings 6-17 (#671): module-size 28553->28571 (+18). Measured from scratch on both sides of this branch with each side's own allowances in place; main's 28553 cross-checks exactly against the absolute #687 recorded in refactor-668-diff-coverage-split.json. The printed line reads 26965 -> 28571 (+1606), but baselines.json is stale (2026-08-10) and 1588 of that is inherited from main's own allowed growth (#674, #684, #675, #682, #683, #687). This is a PURE RENAME -- zero behaviour change, zero new logic, no new or deleted modules (81 .ts files under ts/src before and after). Every one of the +18 lines is line-wrapping, and it is wrapping the renames force: all twelve make the identifier LONGER (`render` -> `renderFindings`, `probe` -> `probeFramework`, `SOURCE` -> `ANALYSIS_SOURCE`, `CliExit` -> `CliExitError`, `buildSpikesReport` -> `buildFailureSpikesReport` and so on), so call sites and doc comments that previously fit in 80 columns no longer do. Roughly +14 is prettier rewrapping code (analysis/reports.ts +4, history/cli.ts +4, guardian/analysis-emit.ts +3, cli-commands.ts +3; framework-probes.test.ts nets -1 but is test code and not counted), and the remaining +4 is rewrapping ELEVEN doc comments by hand, which prettier does not reflow and which would otherwise have shipped this branch's only 80-column violations. Nothing was absorbed here to dodge a fix. Measured with the CI pin (@harness-engineering/cli@11) on both sides, with this branch's own allowance moved aside for the candidate run so the two are comparable: newViolations is 0 on main and 0 here, thresholdViolations stays 21 on both, resolvedViolations stays 16 on both, and main's regressions list is empty because its own allowances already cover 28553. module-size is the only axis that moved; the entropy ratchet reads 292 findings against a 297 baseline on BOTH sides, unchanged. Behaviour-preservation is evidenced rather than asserted: 132 test files / 2673 passing / 2 skipped, identical to main, with no test edited beyond the identifier substitution itself, plus a differential CLI parity run (10 invocations through a candidate and a baseline build, 771 normalized lines, byte-identical output and exit codes). Test-file growth is not counted by module-size.",
  "categories": {
    "module-size": 28571
  },
  "violationIds": [],
  "createdFrom": "dced1f1"
}
