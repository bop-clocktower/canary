{
  "reason": "JUnit XML ingestion for canary history record (#963, PR #978): module-size 39070->39484 (+414), measured with `harness check-arch --json` (CLI 12.7.0) on this branch rebased onto main 20aa85c, after #972 (history formats split), #975 (py* renames) and #977 (new arch violations now block) merged. newViolations is 0 and the only regression is this aggregate. The growth is the feature: a third report reader, ts/src/history/formats/junit-report.ts (~296 lines), that maps pytest, surefire and other JUnit XML into the run-history store `canary ci-ready` and `canary analyze` read. It handles nested <testsuites>/<testsuite> and a bare <testsuite> root, failure/error/skipped, fractional or absent time -> duration_ms, and three retry encodings (surefire flakyFailure/flakyError, rerunFailure/rerunError, and a repeated classname::name in one report) mapped to the existing `flaky` status. The remaining +40 is the detection branch in run-recorder.ts (+22) and accurate supported-format text in history/cli.ts (+18). No new npm dependency: the Cobertura XML primitives moved from ts/src/guardian/diff-coverage/formats/xml.ts to ts/src/util/xml.ts so the history layer (allowed: core, util) can reuse them without a guardian import; check-deps passes. It fits the arch shape #972 set up for it: readers live in the formats/ subfolder, which the top-level module count does not include, so ts/src/history is 1758 top-level lines, under its 1800 threshold. Splitting would not reduce the aggregate, which scores N files summing to 414 the same as one. Nothing was absorbed here to avoid a fix: the lane parked on this rather than writing it, and the human approved recording the growth.",
  "categories": {
    "module-size": 39484
  },
  "violationIds": [],
  "createdFrom": "20aa85c"
}
