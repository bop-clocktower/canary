Generate a Playwright test for a login page at https://example.com/login. Cover two scenarios in the same file: (1) Happy path — user enters "user@example.com" / "correct-horse-battery-staple", clicks the Sign In button, ends up on /dashboard. (2) Error path — user enters the same email with "wrong-password", clicks Sign In, sees an error message containing "Invalid credentials", and the URL stays on /login. Use page object model where it adds clarity but don't over-engineer. Assertions should be specific (URL, visible text), not loose. No screenshots, no video.
