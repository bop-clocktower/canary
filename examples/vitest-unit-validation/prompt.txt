Generate a Vitest unit test for a function validateEmail(input: string): { ok: boolean; error?: string }. Rules: returns { ok: true } for any string matching "<local>@<domain>.<tld>" with non-empty local/domain and tld of length >= 2; returns { ok: false, error: "empty" } for "" or whitespace-only; returns { ok: false, error: "invalid" } for anything else (missing @, no tld, etc.). Cover at minimum: valid ("user@example.com", "a.b+c@sub.example.co.uk"); empty ("", "   "); invalid ("user@", "user@example", "@example.com", "user.example.com", "user@@example.com"). Use describe/it blocks. No snapshot testing — assertions only.
