{
  "type": "object",
  "required": ["amountCents", "rate", "currency"],
  "properties": {
    "amountCents": { "type": "integer" },
    "rate": { "type": "number", "minimum": 0, "maximum": 1 },
    "currency": { "type": "string", "enum": ["XTS"] },
    "memo": { "type": "string", "maxLength": 40 }
  }
}
