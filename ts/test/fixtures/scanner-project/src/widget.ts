export class Button {
  label = 'click';
}

export function helper(x: number): number {
  return x + 1;
}

export const Card = () => 'card';

export const util = 42;

router.get('/items', () => []);
router.post('/items', () => ({}));
