name: Dogfood

# Canary, pointed at canary.
#
# The repo already dogfooded three things (guardian pr-check, blackhawk and
# savant at --strict). This workflow covers the rest of the product surface,
# because a test-intelligence tool that has never read its own tests is making a
# claim it cannot support.
#
# It paid for itself before it shipped: running `review-test` over canary's own
# suites for the first time reported 216 assertion-free tests of which 13 were
# real -- a `node:assert` blind spot and a fixed-window body scan, both fixed in
# #535. That is the point of this file. Findings here are DATA about the
# product, not just chores.
#
# Posture: everything is ADVISORY and annotated. Nothing here blocks a merge
# yet. The ratchet to `--strict` happens per-check once its findings are
# triaged to zero, exactly how blackhawk and savant got there (#485 -> #493 ->
# strict). Promoting a check before its precision is known is how you teach
# people to ignore it (ADR 0010).

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  # --- A: canary's test-quality linter, pointed at canary's tests ------------
  test-quality:
    name: Test quality (canary on canary)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc

      - name: Build the engine
        run: |
          npm --prefix ts ci
          npm --prefix ts run build

      # Exit 3 (abstained) is the interesting failure here: it means the path
      # matched zero test files, i.e. this step's own configuration broke. That
      # must NOT read as a clean lint run -- which, before #508, it would have.
      # Findings themselves (exit 1) are reported and counted, not fatal.
      #
      # `vacuity-check` joined this loop in #716. It shipped in #714 and spent
      # its first days running nowhere, which is the exact claim this workflow's
      # header says a test-intelligence tool cannot make: it had never read our
      # own tests. Its contract differs from its neighbours -- findings exit 0
      # (advisory, per ADR 0009's advisory/gate split) while a collapsed
      # denominator still exits 3 -- so the abstention guard below covers it
      # unchanged, and the count comes from `--json` either way. Baseline at
      # time of wiring: 101 findings (27 ts/test, 2 npm, 72 agents/skills/test),
      # triage tracked in #705/#706.
      #
      # After that triage the vacuity half reads 19 (15 ts/test, 2 npm, 2
      # agents/skills/test), all `VAC-002` at `import-inferred` fidelity;
      # `VAC-001`, `VAC-003` and `SOUND-*` are at zero across all three trees.
      # The bulk of the drop was the inference learning to SEE namespace
      # imports, dynamic imports and subprocess targets (#705) rather than any
      # rule being narrowed -- the finding count fell because the false
      # positives were false, and VAC-002 still fires on the residue.
      #
      # NOT ratcheted to strict here, and the reason is mechanical rather than
      # a lack of appetite: this step counts every rule from three commands into
      # one number, so there is no per-rule gate to promote `SOUND-*`/`VAC-001`
      # through while `VAC-002` (per #705) and `review-test`'s LINT-006 backlog
      # are deliberately still advisory. Promoting needs a per-rule count first.
      #
      # `promote-check` is deliberately NOT here. It is a single-file gate and
      # abstains (exit 3) on a directory, so adding it would manufacture a
      # permanent "Dogfood misconfigured" error rather than coverage.
      # `ts/test/workflow-false-green.test.ts` pins both halves of that.
      - name: review-test + flake-check + vacuity-check over canary's own suites
        run: |
          set +e
          broke=0
          total=0
          for dir in ts/test npm/scripts/__tests__ agents/skills/test; do
            for cmd in review-test flake-check vacuity-check; do
              echo "::group::canary $cmd $dir"
              node ts/bin/canary.js "$cmd" "$dir"
              rc=$?
              echo "::endgroup::"
              if [ "$rc" -eq 3 ]; then
                echo "::error title=Dogfood misconfigured::$cmd ABSTAINED on $dir - it matched zero test files. This is not a clean lint run."
                broke=1
                continue
              fi
              # Two --json shapes are in play. `review-test` and `flake-check`
              # emit a bare array; `vacuity-check` emits an envelope
              # {checked, abstained, findings, skipped} that carries its own
              # denominator. Reading `.length` off the envelope yields
              # `undefined`, which shell arithmetic then quietly treats as 0 --
              # the check would have run, reported nothing, and looked clean.
              # An unreadable count now trips `broke` instead of counting zero,
              # for the same reason exit 3 does: absence of a number is not
              # evidence of zero findings (#508, #716).
              n=$(node ts/bin/canary.js "$cmd" "$dir" --json 2>/dev/null \
                    | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{try{const j=JSON.parse(s);const f=Array.isArray(j)?j:j.findings;console.log(Array.isArray(f)?f.length:"ERR")}catch{console.log("ERR")}})')
              case "$n" in
                '' | *[!0-9]*)
                  echo "::error title=Dogfood count unreadable::$cmd on $dir yielded no countable findings list. A missing count must not read as zero."
                  broke=1
                  continue
                  ;;
              esac
              echo "$cmd $dir: $n finding(s)"
              total=$((total + n))
            done
          done
          echo "DOGFOOD_TOTAL=$total" >> "$GITHUB_ENV"
          exit "$broke"

      # The count is the point: it is the number this check will be ratcheted
      # against. Advisory until it is triaged to zero, then promoted -- the
      # blackhawk/savant path (#485 -> #493 -> strict).
      - name: Report the finding count
        run:
          echo "::warning title=Test-quality findings (advisory)::canary
          reported $DOGFOOD_TOTAL finding(s) across its own suites. Non-blocking
          while precision is triaged; see the grouped step logs."

  # --- B: doctor, from a real consumer install ------------------------------
  doctor:
    name: Doctor (from a packed install)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc

      # Deliberately NOT `node ts/bin/canary.js`: this installs the packed
      # tarball exactly as a consumer would, so the bundled engine, the bin
      # shims, and `package.json#files` are all exercised. A regression that
      # only shows up in the published artifact is invisible from the checkout.
      - name: Pack and install the npm package
        run: |
          npm --prefix npm ci
          npm --prefix npm run build
          npm pack ./npm --pack-destination /tmp
          npm i -g /tmp/canary-test-cli-*.tgz

      - name: canary doctor
        run: |
          set +e
          canary doctor
          rc=$?
          set -e
          # Exit 3 = abstained: zero checks were runnable, so doctor verified
          # nothing (#508 Wave 3). On a bare runner with no overlay installed
          # that is a plausible, honest outcome -- it is reported, not hidden,
          # and not treated as a pass.
          if [ "$rc" -eq 3 ]; then
            echo "::warning title=Doctor abstained::canary doctor verified zero checks (exit 3) on this runner. Not a pass."
            exit 0
          fi
          exit "$rc"

      - name: canary doctor --json carries its denominator
        run: |
          canary doctor --json > doctor.json || true
          node -e '
            const r = JSON.parse(require("fs").readFileSync("doctor.json", "utf8"));
            for (const k of ["checked", "skipped", "abstained", "allPassed"]) {
              if (!(k in r)) { console.error(`missing key: ${k}`); process.exit(1); }
            }
            if (r.abstained && r.allPassed) {
              console.error("contract violation: abstained runs are never allPassed");
              process.exit(1);
            }
            console.log(`doctor: checked=${r.checked} skipped=${r.skipped.length} abstained=${r.abstained}`);
          '

      # Same rule as harness.yml (#588): a report the job's outcome depends on
      # has to outlive the runner, or on the failing run — the only one anyone
      # needs it for — it is as unreachable as if it were never written.
      - name: Upload doctor report
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v7
        with:
          name: doctor-report
          path: doctor.json

  # --- E: the declared Node floor is enforced, not just declared ------------
  #
  # `engines` is ADVISORY. Installing a package that declares
  # `engines.node: ">=99"` on a newer Node exits 0 with only
  # `npm warn EBADENGINE`; it errors only under `engine-strict=true`, which
  # almost nobody sets. So `>=22` in the manifest stops nobody, and before #559
  # a Node 20 user's first signal was whatever SyntaxError the engine happened
  # to throw -- which reads as a canary bug, not as "wrong Node".
  #
  # This job installs the PACKED TARBALL on a deliberately unsupported Node and
  # asserts the guard fires with a message a human can act on. It is the only
  # place that combination is ever exercised: job D covers the supported floor,
  # and no unit test can prove the guard survives being placed before the
  # engine require in a real install.
  node-floor:
    name: Node floor is enforced (unsupported runtime)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      # Build on the supported version -- we are testing the RUNTIME guard, not
      # whether the engine compiles on an old Node (it does not, by design).
      - uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc

      - name: Pack the npm package
        run: |
          npm --prefix npm ci
          npm --prefix npm run build
          npm pack ./npm --pack-destination /tmp

      - name: Derive the unsupported version from the declared floor
        id: floor
        run: |
          floor=$(node -e "console.log(/^>=\s*(\d+)/.exec(require('./npm/package.json').engines.node)[1])")
          echo "floor=$floor" >> "$GITHUB_OUTPUT"
          echo "unsupported=$((floor - 2))" >> "$GITHUB_OUTPUT"
          echo "declared floor: $floor, testing against: $((floor - 2))"

      # Switching Node AFTER packing: the tarball is already built.
      - uses: actions/setup-node@v7
        with:
          node-version: ${{ steps.floor.outputs.unsupported }}

      - name: Install on the unsupported runtime
        # The install itself must still SUCCEED -- that is the whole point.
        # npm warns and proceeds, so the guard is the only real protection.
        run: npm i -g /tmp/canary-test-cli-*.tgz

      - name: The guard fires with an actionable message
        # FLOOR comes through env rather than being interpolated into the shell.
        # The regex above captures digits only, so injection is not reachable
        # today -- but a `${{ }}` inside `run:` is the pattern that goes wrong
        # later, and this repo gates on that class (#549 era workflow hygiene).
        env:
          FLOOR: ${{ steps.floor.outputs.floor }}
        run: |
          set +e
          out=$(canary --version 2>&1)
          rc=$?
          set -e
          echo "$out"
          echo "exit=$rc"

          if [ "$rc" -eq 0 ]; then
            echo "::error title=Node floor not enforced::canary ran on Node $(node -v), below the declared floor of ${FLOOR}. engines is advisory, so the runtime guard in bin/canary.js is the only thing protecting this -- it did not fire."
            exit 1
          fi

          # A SyntaxError means the engine was required BEFORE the guard ran, so
          # the guard is dead code on exactly the versions it exists to catch.
          if echo "$out" | grep -qiE 'syntaxerror|unexpected token'; then
            echo "::error title=Guard ran too late::canary failed with a SyntaxError instead of the Node-floor message. The guard must run before the require of dist/router.js."
            exit 1
          fi

          if ! echo "$out" | grep -q "requires Node ${FLOOR}"; then
            echo "::error title=Unhelpful failure::canary failed on an unsupported Node but did not say so. Expected the floor message naming Node ${FLOOR}."
            exit 1
          fi

          echo "Guard fired correctly on Node $(node -v)."

  # --- B2: the skills' own declared surface, verified against reality -------
  skill-surfaces:
    name: Skill surface integrity
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc

      # The examples half SPAWNS the CLI, so it needs a real build. Without
      # one, `skills verify` abstains and says why rather than reporting a
      # clean corpus it never executed.
      - name: Build the engine
        run: |
          npm --prefix ts ci
          npm --prefix ts run build

      # Two denominators, checked separately. `skills verify` is advisory and
      # exits 0 on findings, so this step reads the JSON and decides: a
      # collapsed denominator on EITHER half is an error annotation, because a
      # sweep that inspected nothing has abstained, not passed (#508). Findings
      # themselves are warnings while precision is unknown (ADR 0010).
      - name: Verify skill surfaces and execute documented examples
        run: |
          node ts/bin/canary.js skills verify --root . --json > surfaces.json
          node -e '
            const r = JSON.parse(require("fs").readFileSync("surfaces.json", "utf8"));
            let broke = 0;
            for (const half of ["surfaces", "examples"]) {
              const { checked, abstained, findings } = r[half];
              console.log(`${half}: checked=${checked} findings=${findings.length}`);
              if (abstained) {
                console.log(`::error title=Skill surface check abstained::the ${half} half verified ZERO items, so it measured nothing. This is not a pass.`);
                broke = 1;
              }
              for (const f of findings) {
                console.log(`::warning title=${f.kind}::${f.skill ?? f.name}: ${f.detail}`);
              }
            }
            const skipped = r.examples.skipped.length;
            if (skipped) console.log(`::notice title=Unverifiable examples::${skipped} documented example(s) could not be executed; each carries its reason in surfaces.json.`);
            process.exit(broke);
          '

      # The report has to outlive the runner (#588): on the failing run — the
      # only one anyone needs it for — an un-uploaded report is unreachable.
      - name: Upload the surface report
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v7
        with:
          name: skill-surface-report
          path: surfaces.json

  # --- C: fleet health, on canary's own runs --------------------------------
  fleet-health:
    name: Fleet health (canary's own runs)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc

      - name: Build the engine
        run: |
          npm --prefix ts ci
          npm --prefix ts run build

      # Records a REAL run into canary's own history store. Until this existed,
      # every analyze/history command had only ever run against synthetic
      # fixtures -- including the runs-vs-rows denominator design, whose whole
      # point is telling a healthy fleet apart from an empty one. That
      # distinction cannot be trusted until it has seen data nobody hand-made.
      #
      # This was `scripts/dogfood-record-run.mjs` until #538 promoted it to a
      # real subcommand -- so this step now dogfoods the SHIPPED writer rather
      # than repo-local glue. `record` exits 3 (abstained) when the report
      # carries zero results, which fails the step: a missing report and an
      # empty suite both mean the analyze step below would have nothing to read.
      - name: Run the suite and record it
        run: |
          npm --prefix ts run test -- --reporter=json --outputFile=/tmp/vitest.json || true
          node ts/bin/canary.js history record /tmp/vitest.json --suite ts-engine

      - name: analyze over the recorded run
        run: |
          set +e
          node ts/bin/canary.js analyze flaky
          rc=$?
          set -e
          # With a run recorded this MUST NOT abstain. If it does, either the
          # recorder wrote nothing or the store path drifted -- and the
          # abstention is the only reason we would find out.
          if node ts/bin/canary.js analyze flaky | grep -qi 'abstained'; then
            echo "::error title=Fleet health broken::analyze abstained immediately after a run was recorded. The recorder or the store path is broken."
            exit 1
          fi
          exit "$rc"

      - name: history summary reports a real pass rate
        run: node ts/bin/canary.js history summary ts-engine

  # --- D: deleted-test guard ------------------------------------------------
  katana:
    name: Deleted-test guard (canary-katana)
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc

      - name: Fetch the base branch
        env:
          BASE: ${{ github.base_ref }}
        run: git fetch --no-tags origin "$BASE"

      # Advisory: capturing a deleted test is information, not a veto. Run with
      # --strict so a real capture is distinguishable from a clean run, then
      # handle the two non-zero codes SEPARATELY -- 1 means "a test was
      # deleted", 3 means "the diff was empty and nothing was examined". Letting
      # `continue-on-error` blur them would annotate an empty diff as a deletion,
      # which is the exact conflation #508 exists to end.
      - name: Capture deleted tests
        env:
          BASE: ${{ github.base_ref }}
        run: |
          git diff "origin/$BASE...HEAD" > /tmp/pr.diff
          set +e
          node agents/skills/claude-code/canary-katana/scripts/cli.mjs \
            --repo . --diff-file /tmp/pr.diff --no-write --strict
          rc=$?
          set -e
          case "$rc" in
            0) echo "katana: no deleted tests in this PR." ;;
            1) echo "::warning title=Deleted tests (advisory)::canary-katana captured removed test coverage in this PR. Non-blocking - see the step log." ;;
            3) echo "::notice title=Katana abstained::the PR diff was empty for katana, so nothing was examined. Not a clean run, but nothing to report." ;;
            *) echo "::error title=Katana failed::unexpected exit $rc"; exit "$rc" ;;
          esac
